{{<layout}}
{{$content}}
    <h1>{{ book.title }}</h1>
    <img src="{{ book.thumbnail }}"/>
    <esi:include src="http://book-inventory-s.herokuapp.com/stock/{{ book.isbn }}"/>
{{/content}}
{{/layout}}
